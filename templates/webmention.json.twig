{% set mention = page.header.webmention %}
{% set payload = {
    mentioner: mention.mentioner,
    mentionee: mention.mentionee,
    date_received: mention.date_received|date('Y-m-d H:i:s'),
    valid: mention.valid,
    approved: mention.approved
} %}
{{ payload|json_encode|raw }}
